<?xml version="1.0" encoding="utf-8" ?>
<d:ViewBase xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="clr-namespace:PetrolPriceMonitor.Views;assembly=PetrolPriceMonitor"
             x:Class="PetrolPriceMonitor.Views.ForgotPasswordView"
             Title="{Binding Title}">
  <StackLayout>
    <Label Text="Forgot password?" HorizontalOptions="Center" />
    <Label Text="Enter your email address below and we'll send you an email with a link to reset your password." LineBreakMode="WordWrap" />
    <Entry x:Name="Email" Text="{Binding Email}" Keyboard="Email" />
    <Button Text="Send" HorizontalOptions="Center" Command="{Binding SendCommand}" />
</StackLayout>
</d:ViewBase>